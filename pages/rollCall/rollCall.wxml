<!--pages/rollCall/rollCall.wxml-->
	<view class='map-container'>
		<map class='map' id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" show-location='{{true}}' controls='{{controls}}' bindcontroltap="controltap" markers="{{markers}}" bindmarkertap='markertap'></map>
	</view>

<view class="rC_page">
	<view class='rC_p rC'>点名</view>
	<view class='rC_p sI'>
		<navigator url="/pages/signIn/signIn">签到</navigator>
	</view>
	<view class='rC_p lea'>请假</view>
	<view class='rC_p rec'>记录</view>
</view>


<!-- .rC_page{
  height: 30%;
  width: 100%;
  right: 0px;
  bottom: 0px;
  left: 0px;
  display:flex;
  position: absolute;
  flex-direction: column;
  align-items:center;/*垂直居中*/
  justify-content: center;/*水平居中*/
}

.rC_p{
  /* 内容： */
  height: 10%;
  width: 48%;
  /* 边框*/
  border: 1rpx dotted black;
  /* 内边距： */
  /* padding: 40rpx;*/
  margin: 8rpx auto; 
  color: #13227a;
  /* 边框做圆角显示 */
  border-radius: 40rpx;
} -->